{% macro printRow(form) %}
    <div class="box box-primary">
        <div class="box-body box-profile" data-controller="client-car-select2-datepicker">
            {{ form_widget(form) }}
            <button type="button" class="btn btn-danger"
                    data-action="click->form-collection#removeFormFromCollection">
                Odstranit
            </button>
        </div>
    </div>
{% endmacro %}
