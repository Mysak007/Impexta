{% macro printRow(form) %}
        <div class="box box-primary">
            <div class="box-body box-profile">
                {{ form_widget(form) }}
                <button type="button" class="btn btn-danger remove-file-row">
                    Odstranit
                </button>
            </div>
        </div>
{% endmacro %}
